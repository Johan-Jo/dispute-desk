/**
 * Inner width + horizontal padding for public marketing chrome.
 * Must match the header wrapper in MarketingSiteHeader so the logo
 * and page content share the same left edge on wide viewports.
 */
export const MARKETING_PAGE_CONTAINER_CLASS =
  "max-w-[1440px] mx-auto px-5 sm:px-10 lg:px-14";
