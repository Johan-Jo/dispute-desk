/** localStorage + first-party cookie value when the user accepts analytics. */
export const CONSENT_VALUE_ANALYTICS = "v1:analytics";

/** User chose essential / non-analytics only — banner dismissed without granting GA. */
export const CONSENT_VALUE_ESSENTIAL = "v1:essential";

export const CONSENT_STORAGE_KEY = "dd_cookie_consent";

export const CONSENT_COOKIE_NAME = "dd_cookie_consent";
